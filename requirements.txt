# Leitura dos PDFs
PyMuPDF>=1.24

# OCR das notas digitalizadas
# (precisa tambem do programa Tesseract instalado no computador --
#  veja o README)
pytesseract>=0.3.10
Pillow>=10.0

# Geracao da planilha
pandas>=2.0
openpyxl>=3.1

# Opcional: permite arrastar e soltar os PDFs na janela.
# Sem ele a interface funciona normalmente, usando os botoes.
# tkinterdnd2>=0.4
